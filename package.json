{
  "name": "agent-store",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "clean": "turbo clean",
    "crawl:catalog": "bun scripts/crawl-catalog.ts"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@11.5.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}

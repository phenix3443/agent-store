{
  "name": "@as/cli",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:bin": "bash scripts/build-bin.sh",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@as/client-core": "workspace:*",
    "@as/types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
